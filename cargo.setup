defaults:
  metal: development
commands:
    install:
        -
            module: cargo-git
            action: cloneAll
            data: config.codebases
cli:
    loglevel: error # log, error, throw, none
